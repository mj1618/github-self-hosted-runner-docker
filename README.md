# Github Self-hosted Runner with Docker

1. Set `REPO` and `TOKEN` in `.env` (repo is user/reponame and token is from github action settings -> create new self-hosted runner)
2. `docker compose up -d`
