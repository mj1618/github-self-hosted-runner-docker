# Github Self-hosted Runner with Docker

1. Set `REPO` to `user/projectname` in `.env`
2. Create a personal access token with Administrative write-access on the repo and set `TOKEN` to this value in `.env`
3. `docker compose up -d`
